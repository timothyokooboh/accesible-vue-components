describe("Breadcrumb", () => {
  it("is accessible", () => {
    render(Breadcrumb);
  });

  it("has the proper aria attribute", () => {
   
  });

  describe("breadcrumb separator", () => {
    it("can be changed to a custom separator", () => {
    });
  });

  describe("BreadcrumbItem", () => {
    describe("current page", () => {
      it("has default unique appearance", () => {
      });

      it("can have a custom unique appearance", () => {
      });
    });
  });
});
